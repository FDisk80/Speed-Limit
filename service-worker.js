// This is a basic, empty service worker.
// Its presence is enough to make the web app installable.
self.addEventListener('fetch', (event) => {
  // We are not adding any caching logic here for simplicity.
  // The browser will handle fetching resources as usual.
});
